# mlopezmad Web V2.9 remates de marca sobre GitHub actual 04/07/2026

Base usada: ZIP actual de GitHub `mlopezmad-main actual 040726.zip`.

Cambios aplicados:

- Editor’s Choice:
  - `SELECCIÓN DEL AUTOR` se mantiene como etiqueta superior.
  - subtítulo cambiado a `Fotografías que permanecen.` para evitar duplicidad.

- iPhone 4s:
  - eliminado el pie de foto duplicado bajo la imagen principal.

- Series iPhone 4s:
  - Cáceres y Cádiz quedan con el mismo subtítulo:
    `iPhone 4s · Transfer Filter Project`.

- Marca:
  - añadido símbolo pequeño `assets/logo-mark.png` junto a `mlopezmad` en cabeceras interiores.
  - el símbolo cambia automáticamente con modo claro/oscuro.
  - añadido detalle muy sutil del símbolo dentro del desplegable móvil.

No se han modificado Studio, Dashboard, Worker, `collections.json`, galerías JSON ni fotografías.
