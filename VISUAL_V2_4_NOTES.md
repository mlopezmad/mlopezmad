# mlopezmad Web V2.4 visual revision

Cambios incluidos:

- Newsletter: el claim `Sin algoritmos. Sin prisas. Solo fotografía.` pasa debajo del botón `Suscribirme`.
- Menú móvil: botón más pequeño y desplegable con efecto cristal más transparente.
- Home: se elimina el enlace superior redundante `Última publicación`.
- Home: la barra de resumen cambia `Proyecto actual` por `Última publicación` y ese bloque enlaza a la última galería publicada.
- Home: portada dinámica en cada carga, usando imágenes ya publicadas en `collections.json` / galerías, sin tocar Studio ni Worker.
- Hero: se conserva el oscurecimiento/contraste para mantener legible el texto sobre distintas fotos.

No se han modificado Studio, Dashboard, Worker, `collections.json`, `galeria.json` ni la estructura de publicación.
